{
  "buildType": {
    "default": "debug",
    "choices": {
      "debug": {
        "short": "Debug",
        "long": "Build with debug symbols",
        "buildType": "Debug"
      },
      "release": {
        "short": "Release",
        "long": "Build with optimizations",
        "buildType": "Release"
      }
    }
  }
}
